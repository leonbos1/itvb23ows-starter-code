pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                echo 'Building...'
                sh 'php -v'
            }
        }

        stage ('Deploy') {
            steps {
                echo 'Deploying...'
            }
        }
    }
}